name: demo_workflow
on: workflow_dispatch
jobs:
  build:
    name: build jobs
    runs-on: ubuntu-latest
    steps:
       - name: hello-steps
         run: |
         echo "running demo build."
         echo "runnunng 2nd demo."
